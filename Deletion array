#include<stdio.h>
int main()
{
int a[10],n=7,i,pos;
printf("Enter the array elements:");
for(i=0;i<n;i++)
scanf("%d",&a[i]);
printf("Enter the position of the element to be delete:");
scanf("%d",&pos);
for(i=pos;i<n;i++)
a[i]=a[i+1];
for(i=0;i<n-1;i++)
printf("%d",a[i]);
return 0;
}
